import { Layout } from "../../ui/Layout"

import styles from './index.module.css'

export const Completed = () => {
  return (
    <Layout>
      <h1 className={styles.heading}>完了済み</h1>
    </Layout>
  )
}